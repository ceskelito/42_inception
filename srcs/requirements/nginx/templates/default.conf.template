server {
	listen 443 ssl;
	listen [::]:443 ssl;
	server_name ${BASE_DOMAIN} www.${BASE_DOMAIN};

	ssl_certificate		/run/secrets/${BASE_DOMAIN}.crt;
	ssl_certificate_key	/run/secrets/${BASE_DOMAIN}.key;

	# Set TLS version
	ssl_protocols TLSv1.2 TLSv1.3;
}
